name: Super-Linter

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  super-lint:
    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Lint code base
        run: make lint

